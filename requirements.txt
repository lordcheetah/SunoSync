# SunoSync runtime dependencies.
# Versions are pinned so PyInstaller builds are reproducible.
# To upgrade: bump a pin, reinstall into a clean venv, run `pytest`, then rebuild.

requests==2.32.5
mutagen==1.47.0
Pillow==11.3.0
pyperclip==1.11.0
python-vlc==3.0.21203
customtkinter==5.2.2
appdirs==1.4.4
pypresence==4.6.1
sentry-sdk==2.55.0
pynput==1.8.1

# Windows only: required by core.utils.copy_files_to_clipboard for CF_HDROP.
pywin32==311; sys_platform == "win32"

# NOTE: colorama was previously listed but is imported nowhere in the codebase.
# NOTE: VLC itself is a native dependency and is NOT installable via pip.
#       Install VLC Media Player separately; python-vlc is only the binding.
